// @flow
import React from 'react';

type NewPropertyPageProps = {};

const NewPropertyPage = (props: NewPropertyPageProps) => (
  <div>Hello from NewPropertyPage</div>
);

export default NewPropertyPage;
