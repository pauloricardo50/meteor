// @flow
import React from 'react';

type CommissionConsolidatorProps = {};

const CommissionConsolidator = (props: CommissionConsolidatorProps) => (
  <div>Hello from CommissionConsolidator</div>
);

export default CommissionConsolidator;
