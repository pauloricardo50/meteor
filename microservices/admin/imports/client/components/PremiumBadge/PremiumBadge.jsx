// @flow
import React from 'react';

type PremiumBadgeProps = {};

const PremiumBadge = (props: PremiumBadgeProps) => (
  <span className="premium-badge">Premium</span>
);

export default PremiumBadge;
