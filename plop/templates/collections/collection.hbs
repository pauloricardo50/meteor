import {{schema}} from './schemas/{{schemaFile}}';
import { {{collectionConstant}} } from './{{constantsFile}}';
import { createCollection } from '../helpers/collectionHelpers';

const {{collection}} = createCollection({{collectionConstant}});

{{collection}}.attachSchema({{schema}});
export default {{collection}};