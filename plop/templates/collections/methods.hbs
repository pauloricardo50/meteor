import SecurityService from '../../security';
import {{serviceName}} from '../{{serviceName}}';
import { {{methodInsert}}, {{methodRemove}}, {{methodUpdate}} } from '../methodDefinitions';

{{methodInsert}}.setHandler((context, { {{collectionNameSingular}} }) => {
// Add security checks
// Example
// SecurityService.checkCurrentUserIsAdmin();
return {{serviceName}}.insert({{collectionNameSingular}});
});

{{methodRemove}}.setHandler((context, { {{collectionNameSingular}}Id }) => {
// Add security checks
// Example
// SecurityService.checkCurrentUserIsAdmin();
return {{serviceName}}.remove({{collectionNameSingular}}Id);
});

{{methodUpdate}}.setHandler((context, { {{collectionNameSingular}}Id, object }) => {
// Add security checks
// Example
// SecurityService.checkCurrentUserIsAdmin();
return {{serviceName}}._update({ id: {{collectionNameSingular}}Id, object });
});