import {{collection}} from './{{collectionName}}';
import CollectionService from '../helpers/CollectionService';

// In 'core/api/api-server' don't forget to
// import {{serviceName}} from './{{collectionName}}/{{serviceName}}'
// and append
// [COLLECTIONS.{{collectionConstant}}]: {{serviceName}} to 'Service' object

class {{serviceName}} extends CollectionService {
constructor() {
super({{collection}});
}
}

export default new {{serviceName}}();