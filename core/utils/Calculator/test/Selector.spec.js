// @flow
/* eslint-env mocha */
import { expect } from 'chai';

import Calculator from '..';

describe('Calculator Selector', () => {
  let params;

  beforeEach(() => {
    params = { loan: { structure: {} } };
  });
});
