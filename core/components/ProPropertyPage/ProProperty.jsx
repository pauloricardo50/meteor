// @flow
import React from 'react';

type ProPropertyProps = {};

const ProProperty = (props: ProPropertyProps) => (
  <div className="card1 card-top">Hello from ProProperty</div>
);

export default ProProperty;
