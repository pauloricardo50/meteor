# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

meteor-base@1.0.4               # Packages every Meteor app needs to have
mobile-experience@1.0.4         # Packages for a great mobile UX
mongo@1.1.14                  # The database Meteor supports right now
blaze-html-templates@1.0.4      # Compile .html files into Meteor Blaze views
reactive-var@1.0.11             # Reactive variable for tracker
tracker@1.1.1                   # Meteor's client-side reactive programming library

standard-minifier-css@1.3.2   # CSS minifier run for production mode
standard-minifier-js@1.2.1    # JS minifier run for production mode
es5-shim@4.6.15               # ECMAScript 5 compatibility for older browsers.
ecmascript@0.5.9              # Enable ECMAScript2015+ syntax in app code
shell-server@0.2.1              # Server-side component of the `meteor shell` command

# dev, place this on top to allow meteor toys to work
meteortoys:allthings            # Most awesome dev tools, shows you Session and mongoDB data
stolinski:stylus-multi          # CSS PreCompiler for Stylus
gadicc:blaze-react-component    # Allows using blaze templates in react, should be removed at some point
aldeed:template-extension       # allows Template.xx.replaces, should be removed at some point

# Data
mdg:validated-method            # MDG written server-side validation before storing
check@1.2.4                           # variable type verification for server side validation
session@1.1.7                         # Lets you use Session variables
# edgee:slingshot               # File upload manager

# Routing
kadira:flow-router              # Router

# Internationalization
tap:i18n                        # Handles internationalization in Meteor
tap:i18n-ui                     # Automatically generates a language dropdown based on available language files

# Icons
fortawesome:fontawesome

# User Accounts
accounts-password@1.3.1               # Basic accounts package from Meteor
std:accounts-ui                 # React account component
std:accounts-basic

# UI tricks
#momentjs:moment                 # Handles date and time values and internationalize them
fastclick@1.0.13                       # Removes the 300ms tap delay on touch screens
kadira:dochead                  # Set custom page titles easily


# Production
spiderable@1.0.14-release-testing.0                      # Lets webcrawlers crawl your website to index it, ex: Google
