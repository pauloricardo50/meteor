// import { HttpBasicAuth } from 'meteor/jabbslad:basic-auth';

// adds http authentication for the entire app with given "username, password"
const setupAuth = () => {
  // const basicAuth = new HttpBasicAuth('epotek', 'goforlife');
  // basicAuth.protect();
};

export default setupAuth;
