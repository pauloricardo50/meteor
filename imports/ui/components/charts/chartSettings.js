export const legend = {
  align: 'left',
  verticalAlign: 'bottom',
  layout: 'horizontal',
  itemStyle: {
    fontSize: '14px',
    fontWeight: 300,
    whiteSpace: 'nowrap',
  },
  symbolWidth: 10,
  symbolHeight: 10,
  useHTML: true,
  // labelFormatter() {
  //   return this.y ? `${this.name}` : '';
  // },
};
