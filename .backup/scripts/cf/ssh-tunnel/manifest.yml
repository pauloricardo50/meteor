applications:
  - name: e-potek-backup-manager
    memory: 64M
    instances: 1
    buildpacks:
      - 'https://github.com/cloudfoundry/staticfile-buildpack.git'
    services:
      - mongo-production
      - logs-production
    random-route: true
