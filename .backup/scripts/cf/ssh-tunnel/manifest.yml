applications:
  - name: e-potek-ssh-tunnel-backup-manager
    memory: 64M
    instances: 1
    buildpacks:
      - 'https://github.com/cloudfoundry/staticfile-buildpack.git'
    services:
      - mongo-production
    random-route: true
